# 🚀 Деплой на Render

Репозиторий готов: https://github.com/maksimboyarkin468-ui/tower-bot

## 📋 Шаги для деплоя:

### 1. Откройте Render Dashboard
Перейдите на: https://dashboard.render.com

### 2. Подключите репозиторий GitHub
- Нажмите `New` → `Web Service`
- Выберите `Connect GitHub account` (если еще не подключен)
- Разрешите доступ к репозиториям
- Выберите репозиторий: **`tower-bot`**

### 3. Настройте Web Service

**Основные настройки:**
- **Name:** `tower-bot-telegram` (или любое имя)
- **Region:** Выберите ближайший (Frankfurt, Germany рекомендуется)
- **Branch:** `main`
- **Root Directory:** Оставьте пустым

**Build & Deploy:**
- **Environment:** `Python 3` (автоматически определится из `runtime.txt`)
- **Build Command:** `pip install -r requirements.txt` (из `render.yaml`)
- **Start Command:** `python bot.py` (из `render.yaml`)

**Environment Variables:**
Добавьте следующие переменные (нажмите `Add Environment Variable`):

```
BOT_TOKEN = ваш_токен_бота_от_BotFather
ADMIN_IDS = ваш_telegram_id (например: 1226518807)
CASINO_REF_LINK = ваша_реферальная_ссылка
```

**Advanced Settings:**
- **Health Check Path:** `/health` ✅
- **Auto-Deploy:** `Yes` ✅

### 4. Создайте сервис
- Нажмите `Create Web Service`
- Render начнет сборку (обычно 2-3 минуты)

### 5. Проверьте деплой

**В Render Dashboard:**
- Откройте вкладку `Logs`
- Должно быть сообщение: `ЗАПУСК БОТА TOWER BOT AI`
- Не должно быть ошибок

**Проверьте health check:**
- URL вашего сервиса: `https://tower-bot-telegram.onrender.com/health`
- Должен вернуться: `{"status": "ok", "service": "tower-bot-telegram"}`

**Проверьте бота:**
- Откройте бота в Telegram
- Отправьте `/start`
- Бот должен ответить

---

## ⚙️ Важно:

1. **Файл `render.yaml` уже настроен!**
   - Render автоматически использует настройки из этого файла
   - Переменные окружения нужно ввести вручную в Dashboard (они помечены `sync: false`)

2. **Polling настроен:**
   - Бот использует long polling (60 секунд)
   - Health check `/health` предотвращает "засыпание" сервиса

3. **Изображения отключены:**
   - Бот работает только с текстом
   - Папка `images/` не загружена в репозиторий

---

## 🔧 Если что-то не работает:

**Бот не запускается:**
- Проверьте `BOT_TOKEN` в Environment Variables
- Проверьте логи в Render Dashboard → Logs
- Убедитесь, что все зависимости установлены

**Health check не работает:**
- Убедитесь, что `/health` указан в Health Check Path
- Проверьте логи на наличие ошибок

**Инстанс "засыпает":**
- Health check должен быть настроен: `/health`
- Polling работает автоматически (long polling 60 сек)

---

## ✅ Готово!

После успешного деплоя бот будет работать 24/7 на Render!

🔗 **Репозиторий:** https://github.com/maksimboyarkin468-ui/tower-bot