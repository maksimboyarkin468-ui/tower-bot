# 📚 Инструкция: Создание нового репозитория на GitHub и деплой на Render

## Шаг 1: Создание репозитория через GitHub Desktop

1. **Откройте GitHub Desktop**

2. **Создайте новый репозиторий:**
   - Нажмите `File` → `New Repository` (или `Ctrl + N`)
   - **Name:** `tower-bot`
   - **Description:** `Tower Bot Telegram - Telegram bot for Tower Rush game`
   - **Local Path:** Выберите папку `C:\Users\boiar\tower-bot-telegram`
   - **Initialize this repository with a README:** ❌ **НЕ отмечайте** (у нас уже есть файлы)
   - **Git Ignore:** Python (или оставьте None)
   - Нажмите `Create Repository`

3. **Добавьте файлы в коммит:**
   - В GitHub Desktop вы увидите все файлы из папки
   - Внизу введите сообщение коммита: `Initial commit - Tower Bot with polling support`
   - Нажмите `Commit to main`

4. **Опубликуйте репозиторий на GitHub:**
   - Нажмите `Publish repository` (или `Repository` → `Publish repository`)
   - **Uncheck** "Keep this code private" (если нужен публичный) или оставьте приватным
   - Нажмите `Publish Repository`

✅ **Готово!** Репозиторий создан на GitHub.

---

## Шаг 2: Настройка деплоя на Render

1. **Откройте Render Dashboard:**
   - Перейдите на https://dashboard.render.com
   - Войдите в аккаунт

2. **Создайте новый Web Service:**
   - Нажмите `New` → `Web Service`
   - Если появится опция подключить репозиторий, выберите `Connect GitHub`
   - Нажмите `Connect GitHub account` (если нужно)
   - Выберите репозиторий `tower-bot`

3. **Настройте Web Service:**

   **Основные настройки:**
   - **Name:** `tower-bot-telegram` (или любое другое имя)
   - **Region:** Выберите ближайший регион (например, Frankfurt, Germany)
   - **Branch:** `main` (или `master`)
   - **Root Directory:** Оставьте пустым (если файлы в корне)

   **Build & Deploy:**
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python bot.py`

   **Environment Variables:**
   Добавьте следующие переменные:
   
   ```
   BOT_TOKEN = ваш_токен_бота
   ADMIN_IDS = ваш_telegram_id (например: 1226518807)
   CASINO_REF_LINK = ваша_реферальная_ссылка
   ```

   **Advanced Settings:**
   - **Health Check Path:** `/health` ✅
   - **Auto-Deploy:** `Yes` ✅

4. **Создайте сервис:**
   - Нажмите `Create Web Service`
   - Render начнет сборку и деплой

✅ **Готово!** Бот будет работать на Render с использованием polling.

---

## Шаг 3: Проверка работы

1. **Дождитесь завершения деплоя** (обычно 2-3 минуты)

2. **Проверьте логи:**
   - В Render Dashboard откройте ваш Web Service
   - Перейдите на вкладку `Logs`
   - Убедитесь, что нет ошибок
   - Должно быть сообщение: `ЗАПУСК БОТА TOWER BOT AI`

3. **Проверьте health check:**
   - Откройте URL вашего сервиса: `https://tower-bot-telegram.onrender.com/health`
   - Должен вернуться JSON: `{"status": "ok", "service": "tower-bot-telegram"}`

4. **Проверьте бота в Telegram:**
   - Откройте бота в Telegram
   - Отправьте команду `/start`
   - Бот должен ответить

---

## 📝 Важные замечания

1. **Polling на Render:**
   - Бот использует long polling (60 секунд)
   - Это позволяет Render держать инстанс активным
   - Health check endpoint (`/health`) предотвращает "засыпание" сервиса

2. **База данных:**
   - База данных `tower_bot.db` будет создаваться автоматически
   - ⚠️ **Важно:** На бесплатном плане Render файлы удаляются при перезапуске
   - Для продакшена рекомендуется использовать внешнюю БД (PostgreSQL, MongoDB и т.д.)

3. **Переменные окружения:**
   - Никогда не храните токены в коде
   - Всегда используйте Environment Variables в Render Dashboard

4. **Логи:**
   - Все логи доступны в Render Dashboard → Logs
   - Логи сохраняются в течение определенного времени

---

## 🔧 Альтернативный способ (через render.yaml)

Если у вас есть файл `render.yaml` в репозитории, Render автоматически обнаружит его и использует настройки из файла.

Файл `render.yaml` уже настроен и содержит:
- Web Service конфигурацию
- Environment Variables (sync: false означает, что нужно вводить вручную)
- Health Check Path: `/health`

После создания репозитория на GitHub, Render автоматически применит настройки из `render.yaml`.

---

## ❓ Если что-то не работает

1. **Бот не запускается:**
   - Проверьте логи в Render Dashboard
   - Убедитесь, что `BOT_TOKEN` правильно настроен
   - Проверьте, что все зависимости установлены (`requirements.txt`)

2. **Health check не работает:**
   - Убедитесь, что health check path установлен: `/health`
   - Проверьте, что endpoint `/health` работает (откройте в браузере)

3. **Бот не отвечает:**
   - Проверьте логи на наличие ошибок
   - Убедитесь, что токен правильный
   - Проверьте, что polling работает (должны быть сообщения в логах о получении обновлений)

4. **Инстанс "засыпает":**
   - Убедитесь, что health check path настроен
   - Проверьте, что polling работает (long polling с таймаутом 60 сек)

---

**Удачи! 🚀**